dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Limonad_Android"
include(
        ":app",
        ':core',
        ":features:login:signin",
        ":features:login:signup",
        ":features:login:intro",
        ":features:workspace:workspace-create",
        ":features:workspace:workspace-select",
        ":features:workspace:workspace-add-member",
        ":features:conversation:conversation-add-member",
        ":features:conversation:conversation-list",
        ":features:chat",
        ":features:thread",
        ":navigator",
        ":data:cache",
        ":data:network",
        ":data:repository",
        ":common-jvm",
        ":domain",
        ":safe",
        ":common-entity"
)
